import * as DBAddress from '../database/schema';
import db from '../database/__database__';
import { gte, lte, and } from 'drizzle-orm';


namespace Provinsi {
    async function getManySub(
        idStart: number,
        idEnd: number): Promise<any>
    {
        
    }
}